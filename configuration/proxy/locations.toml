[locations.back_api]
path = "/api"
client_max_body_size = "32MiB"
plugins = ["rateLimit"]
rewrite = "^/api/(?<path>.+)$ /$1"
upstream = "back"

[locations.front]
path = "/"
plugins = ["forFront"]
rewrite = "^/[a-zA-Z0-9/.-]+$ /index.html"
