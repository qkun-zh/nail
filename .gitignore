# --- Rust build artifacts ---
target/
**/target/
dist/
**/dist/

# --- runtime data (agdb db, pdf, search; reseeded at startup) ---
data/

# --- runtime logs (regenerated at startup; never commit) ---
log/

# --- github api token for ci log access (never commit) ---
document/github_token.txt

# --- downloaded tooling (proxy binary is fetched, not vendored) ---
code/proxy/pingap-linux-gnu-x86-full

# --- editor / OS / misc ---
.vscode/
.idea/
.DS_Store
__pycache__/
*.rs.bk

# --- generated coverage reports ---
code/server/tarpaulin-report.html

document/private/
