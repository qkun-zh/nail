# --- Rust build artifacts ---
target/
**/target/
dist/
**/dist/

# --- runtime data (agdb db, pdf, search; reseeded at startup) ---
data/

# --- runtime logs (regenerated at startup; never commit) ---
log/

# --- sensitive configuration (credentials); template: smtp.toml.example ---
configuration/smtp.toml

# --- downloaded tooling (proxy binary is fetched, not vendored) ---
code/proxy/pingap-linux-gnu-x86-full

# --- editor / OS / misc ---
.vscode/
.idea/
.DS_Store
__pycache__/
*.rs.bkcode/back/tarpaulin-report.html
code/back/tarpaulin-report.html
